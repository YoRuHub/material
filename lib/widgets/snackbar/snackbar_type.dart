import 'package:flutter/material.dart';

class SnackBarType {
  static const successColor = Colors.green;
  static const errorColor = Colors.red;
  static const warningColor = Colors.orange;
  static const infoColor = Colors.blue;

  static const successIcon = Icon(Icons.check_circle, color: Colors.white);
  static const errorIcon = Icon(Icons.error, color: Colors.white);
  static const warningIcon = Icon(Icons.warning, color: Colors.white);
  static const infoIcon = Icon(Icons.info, color: Colors.white);
}
